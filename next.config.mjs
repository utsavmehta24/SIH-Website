/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['encrypted-tbn0.gstatic.com', 'engg.dypvp.edu.in', 'media.licdn.com'],
    },
  };
  
  export default nextConfig;
  